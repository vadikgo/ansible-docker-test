---
- hosts: docker
  tasks:
    - name: application service
      docker:
        name: container
        #image: jaspeen/oracle-11g
        image: oraclelinux:6.7
        state: started
        tty: yes
        #ports:
        #  - 8000:80
        #expose:
        #  - 80

- hosts: guest
  tasks:
    - name: inside docker
      shell: hostname
